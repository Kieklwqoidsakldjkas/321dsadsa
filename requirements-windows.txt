# Requirements для Windows с предкомпилированными пакетами
aiogram==3.2.0
aiohttp==3.9.1
aiofiles==23.2.1
python-dotenv==1.0.0
psutil==5.9.6
requests==2.31.0

# Альтернативные пакеты для Windows (без компиляции)
# Если возникают проблемы с cryptography, используйте:
# cryptography==41.0.7 --only-binary=all
